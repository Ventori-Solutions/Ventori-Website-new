---
---
<section id="process" class="section bg-secondary">
  <div class="container">
    <div class="section-head center">
      <h2 class="h2">Our Proven Process</h2>
      <p class="lead muted">A structured approach to ensure successful AI adoption</p>
    </div>

    <div class="process-steps">
      <div class="process-track" aria-hidden="true"></div>

      <div class="process-step">
        <div class="process-bubble">1</div>
        <h3 class="h3">Discovery</h3>
        <p class="muted">Deep dive into your business goals, data landscape, and technical capabilities.</p>
      </div>
            
      <div class="process-step">
        <div class="process-bubble">2</div>
        <h3 class="h3">Design</h3>
        <p class="muted">Create solution architecture with clear success metrics and implementation plan.</p>
      </div>

      <div class="process-step">
        <div class="process-bubble">3</div>
        <h3 class="h3">Implementation</h3>
        <p class="muted">Develop and deploy the solution with iterative testing and validation.</p>
      </div>

      <div class="process-step">
        <div class="process-bubble">4</div>
        <h3 class="h3">Training</h3>
        <p class="muted">Upskill your team to operate and maintain the solution independently.</p>
      </div>

      <div class="process-step">
        <div class="process-bubble">5</div>
        <h3 class="h3">Support</h3>
        <p class="muted">Ongoing optimization, updates, and scaling as your needs evolve.</p>
      </div>
    </div>
  </div>
</section>

<style>/* --- Process (clean) --- */
/* Scope to #process to avoid cross-page collisions */
#process .process-steps{
  position:relative;
  display:flex;
  justify-content:space-between;
  align-items:flex-start;
  flex-wrap:wrap;
  gap:24px;
}

/* Single horizontal track line behind bubbles (desktop only) */
#process .process-track {
  position: absolute;
  top: 22px; /* half bubble height */
  left: 6rem;      /* start at center of first bubble */
  right: 6rem;     /* stop at center of last bubble */
  height: 2px;
  background: var(--accent, #00a8e8);
  opacity: 0.8;
  z-index: 0;
}

#process .process-step{
  position:relative;        /* keep above track */
  z-index:1;
  flex:1 1 180px;
  min-width:160px;
  text-align:center;
  padding:0 8px;
}

#process .process-bubble{
  width:44px;
  height:44px;
  border-radius:999px;
  background:var(--accent, #00a8e8);
  color:#fff;
  display:flex;
  align-items:center;
  justify-content:center;
  font-weight:700;
  margin:0 auto 10px;
  box-shadow:0 4px 16px rgba(var(--accent-rgb, 0,168,232), .35);
}

/* Titles & text */
#process .process-step .h3{ margin:8px 0 6px; }
#process .process-step .muted{
  margin:0;
  color:var(--text-muted, var(--color-text-muted, #9ca3af));
  font-size:0.95rem;
}

/* Mobile: stack steps, hide track */
@media (max-width: 1100px){
  #process .process-track{ display:none; }
  #process .process-steps{
    flex-direction:column;
    align-items:center;
    gap:20px;
  }
  #process .process-step{
    display: flex;
    flex-direction: column;
    align-items:center;
    text-align:center;
    padding:0;
  }
  #process .process-bubble{
    margin:0 0 10px 0;
  }
}

</style>
